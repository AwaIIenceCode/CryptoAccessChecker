const prompt = require("prompt-sync");

//the main method that delegates the entire program
function main()
{

    while (true)
    {
        switch(userChoise)
        {
            case 1:
                break;
            case 2:
                break;
            case 3:
                break;
            case 4:
                return;
            default:
                break;
        }
    }
}